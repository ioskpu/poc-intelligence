# POC Intelligence

ProofOfConsistency Intelligence Platform is a lightweight SaaS interface for
quantitative market intelligence generated by the existing Futures Lab backend.

The platform is not a trading platform. It does not execute trades and does not
provide financial advice.

## Mission

Expose statistical intelligence in a clear dashboard experience:

- Market rankings
- Opportunity rankings
- Pattern discovery results
- Regime analysis
- Quantitative summaries generated outside the frontend

## Local Development

Install dependencies:

```bash
npm install
```

Run the development server:

```bash
npm run dev
```

Open:

- Landing page: `http://localhost:3000`
- Dashboard: `http://localhost:3000/dashboard`

## Build Instructions

Run lint:

```bash
npm run lint
```

Create a production build:

```bash
npm run build
```

Start the production server after building:

```bash
npm run start
```

## Architecture Overview

The application uses Next.js App Router, TypeScript, TailwindCSS and
shadcn-compatible UI primitives.

Primary source layout:

- `src/app`: App Router pages and layouts.
- `src/components`: Shared layout and UI components.
- `src/features`: Product features grouped by domain.
- `src/services/api`: API access boundary.
- `src/types`: Shared TypeScript contracts.
- `src/lib`: Small shared utilities.

All data access must go through `src/services/api`. Phase 1 uses typed mock
data behind that boundary so the future Futures Lab integration can replace the
implementation without rewriting pages or feature components.

## Current Roadmap

- Phase 0: Charter and project direction completed.
- Phase 1: Next.js foundation, landing page, dashboard shell, mock data and API
  abstraction completed.
- Phase 1.1: Repository initialization and governance.
- Phase 2: Integrate live Futures Lab read-only endpoints.
- Phase 3: Add filters, loading states and error states.
- Phase 4: Add historical intelligence views.
- Phase 5: Add test coverage for data transformations and API contracts.
- Phase 6: Prepare production deployment hardening.
- Phase 7: Evaluate account, billing or subscription needs only if explicitly
  approved later.

## Documentation

- [Roadmap](docs/roadmap.md)
- [Architecture](docs/architecture.md)
- [Phase 1 Notes](docs/phase-1.md)

Check out our [Next.js deployment documentation](https://nextjs.org/docs/app/building-your-application/deploying) for more details.
